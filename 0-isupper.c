#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - return character in upper case
 * @c: value of character
 * Return: 1 if c is upper or 0 if otherwise
 */
int _isupper(int c)
{
if (isupper(c))
{
	return (1);
}
else
{
	return (0);
}
return (0);
}
