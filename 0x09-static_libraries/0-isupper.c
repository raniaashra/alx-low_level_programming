#include <stdio.h>
#include <ctype.h>
/**
 * _isupper - check if the character is upper
 * @c: value of char
 * Return: 1 if is upper or 0 if otherwise
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
return (0);
}
