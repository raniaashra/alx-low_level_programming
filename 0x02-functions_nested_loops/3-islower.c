#include "main.h"
/**
 * _islower - print letter lower
 * @c: character is be chaecked
 * Return: 1 for lowercase or 0 for else
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
{
return (0);
}
}
