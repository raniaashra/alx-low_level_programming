#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: Success
 */
int mul(int a, int b)
{
int multiply = a * b;
return (multiply);
return (0);
}

