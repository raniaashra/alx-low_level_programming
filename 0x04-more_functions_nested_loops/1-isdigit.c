#include <stdio.h>
#include <ctype.h>
/**
 * _isdigit - check if it is digit
 * @c: value
 * Return: 1 if c is digit or 0 if otherwise
 */
int _isdigit(int c)
{
if (isdigit(c))
{
return (1);
}
else
{
return (0);
}
return (0);
}
